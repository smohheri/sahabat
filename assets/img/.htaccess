# Prevent directory listing
Options -Indexes

# Allow access to images
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Allow from all
</FilesMatch>

# Deny access to other files
<FilesMatch "\.(php|exe|bat|sh)$">
    Deny from all
</FilesMatch>
